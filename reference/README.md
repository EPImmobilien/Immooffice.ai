# reference/

Ablage für die Referenz-Anwendung (Master-Prompt Abschnitt 0).

Der Inhalt dieses Verzeichnisses ist **bewusst nicht versioniert** (siehe `.gitignore`).
Die Referenz trägt durchgehend Kennzeichen des Referenzunternehmens; ein Mit-
versionieren würde Abschnitt 2 unterlaufen. Sie ist Arbeitsmaterial, kein
Produktbestandteil.

Erwartete Datei: `epworld-index.html` (Kompilat vom 10.08.2026, ~21 MiB).
Auswertung ausschließlich über `scripts/analyse-referenz.sh` — die Datei wird
niemals vollständig geladen. Ergebnisse: `docs/BESTANDSAUFNAHME.md`.
